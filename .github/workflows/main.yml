name: test

on: [push, pull_request]

jobs:
  build:

    runs-on: macOS-10.14
    
    steps:
    - uses: actions/checkout@v1
    - name: Build
      run: swift build -v
    - name: Test
      run: swift test -v
